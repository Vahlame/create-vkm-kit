{
  "//": "Type surface for typescript-eslint (promise rules need type info). Not itself a gate — the CI-gated checkJs pass is tsconfig.checkjs.json, which scopes down to shipped src. But note it EXTENDS this file, so the `strict: false` below is the gate's inherited baseline, not a lint-only setting; tsconfig.checkjs.json re-enables, flag by flag, every strict option measured to cost zero. Tighten strictness there, not here, unless you mean to move the lint surface too.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": [
    "*.mjs",
    "packages/**/*.js",
    "packages/**/*.mjs",
    "scripts/**/*.mjs",
    "evals/**/*.mjs",
    "evals/**/*.cjs"
  ],
  "//exclude": "evals/*/{results,solutions} is committed verbatim model output, exempted from prettier for the same reason (see .prettierignore): it is an experimental record, not source we may reformat or fix. Excluding it here is what lets eslint's ignore of the same paths take effect — a file in `ignores` that is still in the type-aware project is a parser error, not a skip.",
  "exclude": ["**/node_modules", "evals/*/results/**", "evals/*/solutions/**"]
}
