Implement `parseDuration(s)` in modern JavaScript (ESM).

It parses a compact duration string into a total number of seconds.

Format example: `"1h30m"` → `5400`. Units are `h`, `m`, `s`.

Export the function as a named export.
