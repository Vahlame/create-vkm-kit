Implement `mergeIntervals(list)` in modern JavaScript (ESM).

It merges overlapping intervals. Each interval is a two-element array `[start, end]`.

Example: `[[1,3],[2,6],[8,10]]` → `[[1,6],[8,10]]`.

Export the function as a named export.
