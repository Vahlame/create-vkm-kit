docs/legacy/PROMPT_ULTRA_COMPLETO_v1.md
CLAUDE.md
.clinerules
.github/copilot-instructions.md
**/.pytest_cache/**
# Captured model-output deliverables (design-bench eval evidence) — not hand-authored prose,
# never rewritten for style so the sample stays an exact record of what was generated.
evals/design-bench/samples/*.md
# Captured SERP/crawl fixtures for the research eval (ADR-0057) — recorded tool output,
# same contract as above: byte-exact records, never reflowed to please a formatter.
evals/research/fixtures/*.json
# Raw live-bench evidence (subject solutions + graded rows, committed per round) —
# verbatim model output; reformatting would falsify the record.
evals/*/results/**
# Grader-written subject solutions (regenerable from results/*.jsonl via --grade) —
# verbatim model output, never reformatted.
evals/*/solutions/**
# The starter vault written into a user's own directory. This is PAYLOAD, not repo
# prose: what ships is what the user gets, byte for byte, and some of those bytes are
# load-bearing (SESSION_LOG.md ends with a blank line because `vault_append_file`
# appends after it). Same reason the bench evidence above is exempt — reformatting
# would change the artifact, not just its style.
packages/create-vkm-kit/templates/vault/**
