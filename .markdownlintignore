node_modules
**/node_modules/**
# Machine-local agent state (gitignored), including `.claude/worktrees/*` — full nested
# checkouts of this repo. CI never sees them, but locally they make this lint report
# every file once per live worktree, failing on another branch's work-in-progress.
.claude/**
CLAUDE.md
.clinerules
.github/copilot-instructions.md
# Captured model-output deliverables (design-bench eval evidence) — not hand-authored prose,
# never rewritten for style so the sample stays an exact record of what was generated.
evals/design-bench/samples/**
# Research-bench fixtures reproduce raw pipeline output (bare url: seams are the
# POINT — the validator detects them); captured evidence, never linted as prose.
evals/*/fixtures/
# The starter vault written into a user's own directory — payload, not repo prose.
# See the matching note in .prettierignore: these bytes are the contract.
packages/create-vkm-kit/templates/vault/**
